# API base URL (no trailing slash). Use when running against a different host.
# Default in dev: proxy to localhost:8000 via vite.config.ts
# For production or another client: set to your API origin, e.g. https://api.example.com
VITE_API_URL=
